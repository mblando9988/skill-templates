---
name: "{{skill-name}}"
description: "Use when {{trigger conditions for this plugin skill}}"
when_to_use: "{{example requests}}"
allowed-tools: "mcp__plugin_{{plugin-name}}_{{server}}"
---

# {{Skill Title}}

{{Instructions. Plugin MCP tools are named mcp__plugin_<plugin>_<server>__<tool>;
reference plugin files as ${CLAUDE_PLUGIN_ROOT}/<path>.
Rename this folder to the skill name. See templates/skill-md-template.md for the full field guide.}}
